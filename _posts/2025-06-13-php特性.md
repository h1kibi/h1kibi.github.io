---
title: "PHP特性"
date: 2025-06-13 00:00:00 +0800
categories: [Security]
tags: [security, ctf, php]
---

### 1.`preg_match()`特性
数组绕过`preg_match()`
```
if(preg_match("/[0-9]/", $num)){
        die("no no no!");
    }
echo $flag;
```
传入数组`num[]`使其返回值发生错误而为false

正则表达式修饰符
```
i 
不区分(ignore)大小写

m
多(more)行匹配
若存在换行\n并且有开始^或结束$符的情况下，
将以换行为分隔符，逐行进行匹配
preg_match('/^abc$/m','abc\nabc',' ');
这样其实是符合正则表达式的，因为匹配的时候 先是匹配换行符前面的，接着匹配换行符后面的，两个都是abc所以可以通过正则表达式。

s
特殊字符圆点 . 中包含换行符
默认的圆点 . 是匹配除换行符 \n 之外的任何单字符，加上s之后, .包含换行符
preg_match_all('/b./s', 'abggab\nacbs',' ');
这样匹配到的有三个 bg b\n bs

A
强制从目标字符串开头匹配;

D
如果使用$限制结尾字符,则不允许结尾有换行; 

e
配合函数preg_replace()使用, 可以把匹配来的字符串当作正则表达式执行; 
```

`preg_match /e`命令执行
```

```
### 2.`intval()`特性
```
if(intval($num)){
        echo $flag;
}
```
参数`num`若为一个数组，只要数组里面有值，那么不论值的数量，返回值都为1，空数组则返回0
```
if(intval($num,0)===4476){
        echo $flag;
}

*intval( mixed $value, int $base = 10) : int
```
如果 `base` 是 0，通过检测 `value` 的格式来决定使用的进制：  
◦ 如果字符串包括了 “`0x`” (或 “`0X`”) 的前缀，使用 16 进制 (hex)；  
◦ 如果字符串以 “0” 开始，使用 8 进制(octal)；  
◦ 否则，将使用 10 进制 (decimal)。
```
intval('4476.0')===4476    小数点  
intval('+4476.0')===4476   正负号
intval('4476e0')===4476    科学计数法
intval('0x117c')===4476    16进制
intval('010574')===4476    8进制
intval(' 010574')===4476   8进制+空格
```
***
## PHP函数
### 无参数`RCE`
#### 常见函数
```
目录操作：  
1.getcwd() ：函数返回当前工作目录的绝对路径。
playload:echo getcwd();
output:/var/www/html

2.scandir() ：函数返回指定目录中的文件和目录的数组。  
playload:echo scandir(getcwd());
output:Array
(
    [0] => .
    [1] => ..
    [2] => file1.txt
    [3] => file2.jpg
    [4] => subdir
)

3.dirname() ：函数返回去掉文件名之后的目录路径。  
playload:echo dirname("/var/www/html/index.php");
output:/var/www/html/

4.chdir() ：函数动态切换当前的目录。  
playload:chdir('/tmp'); 
切换到 /tmp 目录



数组相关的操作：  
1.end() - 将内部指针指向数组中的最后一个元素，并返回。
2.next() - 将内部指针指向数组中的下一个元素，并返回。
3.prev() - 将内部指针指向数组中的上一个元素，并返回。
4.reset() - 将内部指针指向数组中的第一个元素，并返回。
以end()为例：playload: 
$arr = array('a', 'b', 'c', 'd');
echo end($arr);
output: d

5.each() - 返回当前元素的键名和键值，并将内部指针向前移动。


6.array_shift() - 删除数组中第一个元素，并返回被删除元素的值。  




读文件  
show_source() highlight_file()- 对文件进行语法高亮显示。
readfile() - 输出一个文件。  
file_get_contents() - 把整个文件读入一个字符串中。  
readgzfile() - 可用于读取非 gzip 格式的文件
```